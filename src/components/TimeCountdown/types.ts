export type DateElement = {
    days: number;
    hours: number;
    minutes: number;
    seconds: number;
};

export const defaultDate: DateElement = {
    days: 0,
    hours: 0,
    minutes: 0,
    seconds: 0
}

